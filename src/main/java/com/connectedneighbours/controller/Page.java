package com.connectedneighbours.controller;

/**
 * Identifie la page actuellement affichée, pour mettre en évidence
 * le bouton correspondant dans le header mutualisé.
 */
public enum Page {
    DASHBOARD,
    INCIDENTS,
    USERS,
    STATISTICS,
    SETTINGS
}
